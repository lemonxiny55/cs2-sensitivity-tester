# CS2 Sensitivity Tester — 概念设计契约(Concepts Design System)

本文件是 `concepts/` 下三个**纯视觉原型(concepts)**的设计契约。三个方向是结构不同的设计语言,不是配色换皮。所有原型为零构建 vanilla HTML/CSS(无 React、无打包器),静态展示,**功能接线明确不在本契约范围内**(见 §8 范围声明)。当前产品 UI(`index.html`/`styles.css`/`ui-previews/`)被用户明确否决,**本文件不从中派生任何审美**。

约定:每个方向拥有独立 token 命名空间(`--bc-` / `--ar-` / `--ro-`),共享一套语义内容块(§5.0)与一套原语状态清单(§5.1)。任何原型代码不得出现 §2 表外的裸 hex。

---

## 0. Research Log

- **Embedded refs (Layer B shortlist)**:候选 Nike(`nike.md`)、Nvidia、Sentry → **选定 gpt-tasteskill(Layer A)+ Nike(Layer B)**。理由:任务是"多个结构不同的方向"的 expressive brief,gpt-tasteskill 提供反均值、反模板的执行纪律(2 行铁律、无空缝网格、meta-label 禁令、无 emoji);Nike 提供大规模全大写展示字 + 严格单色 UI + 颜色只做语义 + 平表面零阴影的克制骨架——正是竞技 FPS 玩家熟悉的"运动/赛事"气质,且只借用其手法、不借用其商标与字体资产。Nvidia(过强的绿黑游戏刻板印象)与 Sentry(开发者错误监控气质,与本工具语义错位)落选。
- **Prior session(上一会话已完成的屏幕研究,记录为既有输入)**:浏览过 Aimlabs 与 cs2practice 的实际界面(布局语法:中央靶场视口 + 侧栏任务列表 + 结果数值面板),并调研过 KovaaK's 的结果呈现(过冲/下冲/命中率作为核心指标词汇)。仅取信息架构与指标词汇的事实,不复制其视觉。
- **ui-ux-db 检索(本会话实际执行)**:`product/real-time operations telemetry status` → 返回 HUD/Sci-Fi FUI、Real-Time Monitor 风格建议(战术色 + 告警色 + 深色底);`typography/gaming esports typography` → 返回 "Gaming Bold" 配对(Russo One + Chakra Petch,电竞气质)与等宽数字标签惯例。两处输入用于方向 C 的字体与方向 A 的遥测数字排印。
- **Lazyweb**:本会话按配方尝试取 token 并查询,**被限流(HTTP 301 至 www 后无可用响应),未取到新屏幕**。真实产品屏幕证据以上一条"prior session"记录为准,不虚构本会话浏览数。
- **Imagen**:本环境不可用,**跳过**,未生成概念草稿。
- **Skipped lanes 汇总**:Lazyweb(限流)、Imagen(工具不可用)。

---

## 1. Atmosphere & Identity

三个方向回答同一个问题——"我的 CS2 灵敏度到底对不对?"——但各自扮演不同的场景角色。**共享人格内核(源自 Nike 的克制):UI 本身几乎单色,颜色永远是语义而非装饰;数据(数字)才是画面主角;排版靠体量、字重和留白施压,不靠花哨。**

### Personas(贯穿三方向的设计对象)

1. **P1 竞技玩家·要信心**:打把排位前 3 分钟测完,只想要一句"这个灵敏度适合你"。需要:一屏一个结论,零决策分支。
2. **P2 分析型玩家·要证据**:会记录过冲率、命中散布,想横向比较最近几轮。需要:密度、表格、可对比的恒定标尺。
3. **P3 疲劳情境·低注意力**:15 分钟测试做完,眼睛累、耐心零。需要:超大可读数字、极少的文字、不强迫精读、色彩独立的状态标记。

三个方向的视觉差异是"同一个工具的三种场景皮肤",信息层级对 P1/P3 必须一致地粗暴清晰;P2 的密度需求通过"渐进展开"(默认结论,展开看明细)满足,而非把所有人淹没在表格里。

### 方向签名(一句话记忆点)

| 方向 | 命名空间 | 场景身份 | 签名瞬间 |
|---|---|---|---|
| **A · Broadcast Grid 赛事导播台** | `--bc-` | 你正在被转播:一块赛事数据导播屏,冷灰演播室,琥珀信号 | 底部横贯的 **Scorebug 裁决条**——赛事转播下三分之一的记分条形态,置信判定以巨型等宽数字 + 队签式色块砸出来 |
| **B · Calibration Archive 校准档案** | `--ar-` | 一本仪器计量手册/野外图鉴:纸墨、细线、图注、页边批注 | **标本卡(Specimen Card)**:本轮结果像一件仪器标本被装裱记录——细线框、图注引线、编号戳,纸面上唯一的颜色是红墨批注 |
| **C · Range OS 靶场座舱** | `--ro-` | 训练舱操作系统开机:深空仪表、透视靶道、冷光边缘 | **透视靶道窗(Tunnel Viewport)**:一个带座舱遮光罩的梯形视窗,远处靶标沿灭点收缩,HUD 角括号环视四周 |

- **A 是Nike 骨架的正面执行者**:平表面、细线分隔、全大写 overline、单色 + 语义色,把"赛事转播记分条"作为唯一的高光结构件。
- **B 刻意反 Nike**:暖纸、衬线、细引线——给 P2 的"证据感"皮肤,但保留"UI 单色、颜色只做语义"的内核(红墨=批注/告警)。
- **C 是 gpt-tasteskill 的电影化侧**:径向光晕、暗角、发光仪表,给 P1 的"进入训练"仪式;但仍禁止玻璃拟态卡片堆。

**禁止(三方向共同)**:通用 AI SaaS 玻璃卡(backdrop-blur 卡片堆)、紫蓝渐变、圆角泛滥(大面板圆角 ≤ 各方向规定值,图片/数据区允许 0 圆角)、emoji(状态一律文字标签 + 几何符号 ▲ ● ◆ ✕)、任何 Aimlabs/KovaaK's/Nike 商标、字样、logo 或品牌专属文案、从现有产品 UI 派生的样式。

---

## 2. Color

每个方向一套独立调色板。三方向共同的语义角色(状态)统一为五类:**ok(判定通过)/ warn(需注意)/ danger(判定不通过)/ info(中性提示)/ live(进行中信号)**。状态色**永远伴随文字标签与形状符号**,不允许仅靠颜色传达(§8)。

### 2.1 A · Broadcast Grid(`--bc-`)

演播室冷灰:深炭蓝灰底,表面层级靠 2–3% 亮度台阶;唯一暖色是琥珀信号色(队签、进行中、裁决强调)。

| 角色 | Token | 值 | 用法 |
|---|---|---|---|
| 画布 | --bc-canvas | #0B0D11 | 页面底 |
| 面板 | --bc-panel | #12151B | 内容面板 |
| 面板凸层 | --bc-panel-hi | #181C24 | 活动面板/悬停底 |
| Scorebug 底 | --bc-score | #1B202A | 裁决条(全系统最亮中性面) |
| 主文本 | --bc-text | #ECEEF2 | 标题/正文 |
| 次文本 | --bc-text-2 | #A6ACB8 | 标签/说明(对 --bc-panel ≈ 6.9:1 ✓AA) |
| 弱文本 | --bc-text-3 | #6E7581 | 禁用/水印(仅非必读) |
| 分隔线 | --bc-rule | #262C36 | 1px 网格线/面板描边 |
| 琥珀信号 | --bc-amber | #FFB02E | 进行中、关键强调、焦点环(对 #12151B ≈ 9.9:1 ✓) |
| 琥珀暗 | --bc-amber-dim | #3A2B0E | 琥珀底纹(选中行底) |
| ok | --bc-ok | #3DDC8B | 达标(文字态,配 ▲) |
| warn | --bc-warn | #FFC53D | 边缘区间(配 ◆) |
| danger | --bc-danger | #FF5A52 | 不达标(配 ✕) |
| info | --bc-info | #5FA8FF | 中性提示(配 ●) |

规则:琥珀是"信号"不是"品牌色"——只出现在 live 状态、焦点环、裁决条强调,面积 ≤ 5%;蓝/绿/红只出现在状态词。

### 2.2 B · Calibration Archive(`--ar-`)

档案纸墨:暖纸底、碳墨字、发丝线;唯一的"彩色"是红墨批注笔。

| 角色 | Token | 值 | 用法 |
|---|---|---|---|
| 纸 | --ar-paper | #F6F2E8 | 页面底 |
| 纸亮 | --ar-paper-hi | #FDFBF5 | 标本卡纸面(比页纸更亮一档) |
| 纸影 | --ar-paper-lo | #EDE7D8 | 页眉/表头等压深纸 |
| 墨 | --ar-ink | #191611 | 标题/正文(对纸 ≈ 15.9:1 ✓) |
| 淡墨 | --ar-ink-2 | #5C564A | 图注/次文(≈ 5.8:1 ✓) |
| 发丝线 | --ar-hair | #C9C1AC | 细线框/引线/表格线 |
| 红墨 | --ar-red | #A83A1C | 批注、告警、编号戳(≈ 5.9:1 ✓) |
| ok | --ar-ok | #1F6B3F | 达标批注(配 ▲ 与文字) |
| warn | --ar-warn | #8A5A00 | 注意批注(配 ◆) |
| danger | --ar-danger | #A83A1C | 与红墨同值:档案里"危险即红笔批注" |
| info | --ar-info | #1D4E79 | 蓝黑铅笔注(配 ●) |

规则:纸面色阶只有三档;任何元素不加阴影(纸是平的,深度只来自引线,§7)。

### 2.3 C · Range OS(`--ro-`)

夜靶场:近黑蓝底,冷青 HUD 光,中心亮边缘暗(径向),警示黄与告警红只在仪表告警时点亮。

| 角色 | Token | 值 | 用法 |
|---|---|---|---|
| 深空 | --ro-canvas | #05080C | 页面底(径向渐亮向中心至 #0A1119) |
| 舱面 | --ro-hull | #0C141D | 仪表面板 |
| 舱面凸层 | --ro-hull-hi | #131E2A | 按钮/控件底 |
| 文本 | --ro-text | #DDE6EE | 主文本(对舱面 ≈ 12.6:1 ✓) |
| 次文本 | --ro-text-2 | #8FA1B3 | 标签(≈ 6.2:1 ✓) |
| HUD 青 | --ro-cyan | #6FE3F5 | 描边发光线、数据高亮、焦点(对深空 ≈ 13:1 ✓) |
| 青暗 | --ro-cyan-dim | #0E3540 | 青色描边的低亮形态/网格线 |
| 警示黄 | --ro-warn | #FFC53D | live 进行中、待命中 |
| ok | --ro-ok | #45E08A | 达标 |
| danger | --ro-danger | #FF4D45 | 不达标/超时 |
| info | --ro-info | #7FA9FF | 中性 |

规则:发光只允许 `text-shadow`/`box-shadow` 形式的**青色窄辉光**(≤ 0 0 12px 4%–16% 浓度),用于 HUD 线与被测量数据;面板不得整体发光成玻璃球。

---

## 3. Typography

**中文现实约束(诚实记录)**:拉丁展示字体无 CJK 字形,界面主体为中文,因此中文标题的"气势"只能靠 **字号、字重(700/800)、行高压缩、字距** 实现;拉丁字形与**数字**(1.0、800、±4.2%——本工具的数据全是数字)才是展示字/等宽字真正出场的地方。三方向的展示字仅用于 ≥ 28px 的标题与数字,永不用于正文。正文中文最小 16px,任何文字不低于 14px。

### 3.1 A · Broadcast Grid

- Display(数字英雄/大写英文标签):**Saira Condensed** 600/700,`text-transform: uppercase`,行高 0.92,字距 -0.01em——赛事记分条的紧凑冲击(Nike 的体量感 + 转播的窄体惯例)。
- UI/正文:**Barlow** 400/500/600(运动排印的中性工作马)。
- 数据数字:**JetBrains Mono** 500/700,`font-variant-numeric: tabular-nums`(第三族的明确理由:记分条与指标面板必须上下对齐,比例数字做不到)。
- CJK 回退栈:`"Barlow","Noto Sans SC","Microsoft YaHei",sans-serif`;中文标题以 Barlow 槽位落到 Noto Sans SC 700。

| 级 | 尺寸 | 字重 | 用途 |
|---|---|---|---|
| stat-hero | clamp(56px, 8vw, 104px)/0.92 | Mono 700 | Scorebug 置信数字 |
| display | clamp(28px, 3.2vw, 44px)/1.0 | Saira Cond 700 大写 | 面板标题 |
| overline | 11px/1.2,+0.10em 大写 | Barlow 600 | 栏目签 |
| body | 16px/1.6 | Barlow 400 | 正文 |
| meta | 13px/1.4 | Mono 500 | 表内数据、时间戳 |

### 3.2 B · Calibration Archive

- Display/正文标题:**Source Serif 4** 600(仪器手册铅字感)。
- 正文/说明:**Source Serif 4** 400(展示与正文同族不同重——档案是"一本书",两族上限留给图注)。
- 图注/数据/编号:**IBM Plex Mono** 400/600(计量表的机械字)。
- CJK 回退栈:`"Source Serif 4","Noto Serif SC","Source Han Serif SC",serif`。
- 层级靠 编号 + 字重 + 引线,几乎不用大字号;最大标题 32px(档案不喊叫,档案陈述)。

| 级 | 尺寸 | 字重 | 用途 |
|---|---|---|---|
| plate-title | 32px/1.25 | Serif 600 | 标本卡题录 |
| h2 | 22px/1.4 | Serif 600 | 章节 |
| body | 17px/1.8 | Serif 400 | 正文(衬线 + 1.8 行高给批注留行间) |
| marginalia | 12px/1.5 | Mono 400 | 页边注、图注、表号 |
| datum | 15px/1.3 | Mono 600 tabular | 指标数值 |

### 3.3 C · Range OS

- Display/UI:**Chakra Petch** 500/600/700(ui-ux-db "Gaming Bold" 建议采纳,科技切角笔划,大写 overline 同 Nike/Saira 传统)。
- 数据数字:**JetBrains Mono** 500/700 tabular(理由:遥测滚动数值必须防抖动)。
- CJK 回退栈:`"Chakra Petch","Noto Sans SC",sans-serif`。
- 座舱内文字少而大:标签 12–13px 大写仅限 HUD 框角,正文 ≥ 16px,核心数值 ≥ 40px——P3 疲劳用户不该在座舱里找小字。

| 级 | 尺寸 | 字重 | 用途 |
|---|---|---|---|
| core-readout | clamp(40px, 6vw, 72px)/0.95 | Mono 700 | 靶道窗中央/主仪表数值 |
| hud-label | 12px/1.2,+0.14em 大写 | Chakra Petch 600 | HUD 角标、仪表签 |
| body | 16px/1.6 | Chakra Petch 400 | 说明文本 |
| stat | 20px/1.2 | Mono 600 tabular | 集群小仪表 |

---

## 4. Spacing & Layout

### 4.1 共享基座

Base **8px**,微距 4px。共享刻度:`--sh-1:4 / --sh-2:8 / --sh-3:12 / --sh-4:16 / --sh-5:24 / --sh-6:32 / --sh-7:48 / --sh-8:64 / --sh-9:96 / --sh-10:128px`。方向可规定自己的"密度系数"(A 收紧、B 松、C 留黑),但取值必须落在该刻度上。`clamp()`、视口单位、`minmax(min(…, 100%), 1fr)` 属于浏览器力学,保持原样不强行 token 化。断点:640 / 768 / 1024 / 1280 / 1536px。375px 下所有布局收敛为单列且主内容无横向滚动;整页包一层防横向溢出的根容器(`overflow-x: clip`)。

### 4.2 A · Broadcast Grid 布局语法

**固定转播壳(shell)**:顶部 40px 状态带(工具名 + 会话计时 + 信号点)→ 主区 12 列稠密网格(栏距 1px 用 `--bc-rule` 画线,而非 gap 黑缝)→ 底部 Scorebug 裁决条(~120px,满宽)。中栏 6 列 = 任务视口;左 3 列 = 任务队列;右 3 列 = 实时指标堆。Bento 排布必须**无缝**:`grid-auto-flow: dense`,col-span 互相咬合,不留空洞格。最大内容 1720px;1024px 以下转单列堆叠,Scorebug 变吸底条。

### 4.3 B · Calibration Archive 布局语法

**单卷手册流**:内容栏 72ch 居中偏左,**左侧 240px 页边注栏**(≥1280px 时)承载 marginalia,窄屏折叠为脚注式行内批注(编号上标 + 文末注)。通栏发丝线(`1px --ar-hair`)分隔"图版",图版内部是标本卡。网格感来自铅字基线对齐的错觉,不用可见网格。垂直节距大(48–96px),像真书的章首留白。

### 4.4 C · Range OS 布局语法

**座舱视口优先**:全屏 100dvh 固定框架——中央梯形遮光罩内是透视靶道窗(占宽 55–70%,灭点略高于几何中心),下缘 25vh 是扇形仪表簇(三到五个圆/弧仪表),左右边缘挂 HUD 角括号控件。**不用卡片网格**;一切信息要么在仪表里,要么在 HUD 框线上。960px 以下仪表簇收为底部两层,靶道窗降到 40vh。

---

## 5. Components

### 5.0 共享语义内容块(三方向同装同序)

先清点内容,再排位置。每个方向都必须容纳全部六块,顺序按"访客决策路径"(钩子 → 参数 → 任务 → 证据 → 结论 → 留存):

| 块 | 职责 | 内容(中文示例文案) |
|---|---|---|
| BLK-1 钩子 | 一句说清这是干什么的 | "3 分钟,测出你最稳的 CS2 灵敏度" |
| BLK-2 参数 | 当前灵敏度 / DPI / eDPI | "灵敏度 1.02 · DPI 800 · eDPI 816" |
| BLK-3 任务 | 测试项目队列 | "甩枪定位 / 平滑追踪 / 爆头线反弹 / 极限甩距" |
| BLK-4 指标 | 分任务与综合指标 | "过冲率 +4.2% · 下冲率 -1.8% · 命中率 63% · 平均反应 312ms" |
| BLK-5 裁决 | 结论 + 推荐区间 | "当前灵敏度偏快 —— 推荐区间 0.95 – 1.00(eDPI 760–800)" |
| BLK-6 留存 | 历史轮次对比 | "对比上一轮:过冲率 +4.2% ↑1.1,置信度 87% ↑5" |

各方向的排版人格可改写措辞强度,但信息角色不得增删,保证三方向可被同一内容公平比较。

### 5.1 可复用原语(方向 × 实例,先建原语展示页再做产品屏)

每个概念目录先交付 `showcase.html`,把该方向全部原语的 default / hover / active / focus-visible / disabled / loading(以静态"进行中"形态呈现)/ empty / error 状态在 375 / 768 / 1280px 下逐一展示,视觉验收通过后才允许组合产品屏。

**NumStat 大数字读数**(三方向各有形态)
- 结构:`figure.stat > b.value(Mono/表格字) + span.unit + p.label`。
- 变体:A 记分条版(48–104px,琥珀/白)/ B 图注版(15–24px,带编号引线)/ C 仪表版(40–72px,青色窄辉光)。
- 状态:hover 仅出现在可点进明细的实例上(下划线或亮度 +6%,禁位移跳动);focus-visible 用各方向焦点环;empty 显示 "--";loading(静态)显示数字灰化 + 各方向的 live 记号。
- 无障碍:数字本体即文本,`aria-label` 带单位;状态不依赖辉光/颜色。
- 布局:stack 原语,scroll owner 为所在面板。

**StatusChip 状态签**
- 结构:`span.chip > i.glyph(▲●◆✕,CSS 形状非 emoji) + 文本`。
- 五语义(ok/warn/danger/info/live)× 三方向形态:A 队签形(4px 圆角 + overline 字)/ B 编号戳形(方框 + Mono)/ C HUD 标签形(切角 `clip-path`)。
- 状态:文本 + 形状永远同色共存 = 色彩独立;disabled 降为 --{ns}-text-3 并去边框。

**VerdictBand 裁决条**(BLK-5 的落位件,签名级)
- A:**即 Scorebug** —— 满宽深面板,左段队签式"推荐 0.95–1.00",中段巨型置信数字 `87%`,右段 live 点 + "比上一轮 ↑5"。
- B:标本卡下方的红墨裁决行:"评定:偏快 —— 建议收敛至 0.95 – 1.00,附注 ③"。
- C:靶道窗正下方主仪表:"判定:偏快"以 core-readout 尺寸点亮,黄色 live 环收拢为绿色 ok 环(静态呈现终态)。
- 三形态共享:结论一句话 + 数字 + 与上轮对比,三元素不得拆分。

**TaskList 任务队列**(BLK-3)
- A:左侧栏 1px 线密排列表,选中行 `--bc-amber-dim` 底 + 琥珀左轨;每行含状态签。
- B:编号图版索引:"3-2 平滑追踪 · 图注①";选中 = 红墨侧线 + 纸亮底。
- C:边缘 HUD 竖列,切角标签堆叠,当前任务青色描边。
- 状态:hover(行底一档提亮,transform 不动)、focus-visible(2px 各方向焦点环,offset 2px)、disabled(任务未解锁:降亮 + 锁形符号 ✕ + 文字"待解锁")、empty("还没有任务 · 点按『新建测试轮』添加")、error(行尾 danger 签 + "该任务本轮数据无效")。
- 键盘:方向键移焦点,Enter 选中;`role="listbox"` 语义由实现期定,原型先保证 Tab 可达。

**MetricPanel 指标面板**(BLK-4)
- A:3×2 无缝 NumStat 网格 + 顶部 overline 栏签;行间用 `--bc-rule`。
- B:计量表:`table` 全发丝线、表头 `--ar-paper-lo`、数字 Mono 右对齐。
- C:仪表簇 3–5 个弧形/环形仪表位(静态 SVG 弧段),读数以 stat 级呈现。
- 空态:"本轮还没有数据 —— 完成第一个任务后在这里出现";误差态:单格转 danger 并显示"采样不足(<30 次)"。

**ParamReadout 参数条**(BLK-2)
- 一行 eDPI 三参数:`灵敏度 1.02 · DPI 800 · eDPI 816`。A 置于顶部状态带;B 置于卷首"仪器铭牌";C 置于 HUD 左上角括号内。纯展示,输入控件不在概念范围。

**HistoryRail 历史轨道**(BLK-6)
- A:Scorebug 右段 ↑/↓ delta + 迷你条形序列(1px 线柱);B:档案末页"轮次登记簿"表;C:仪表簇旁一条水平航迹线,节点为各轮。对比文案统一"对比上一轮"。

**ActionButton 按钮**(概念内为静态视觉态,不接线)
- A:Nike 式全药丸(30px 圆角)、实心 `--bc-amber` 底 + 深字主按钮 / 1px `--bc-rule` 描边次按钮,600 字重,禁 400。
- B:方角(2px)、红墨描边"盖章"按钮:"再测一轮"。
- C:切角(右上/左下 8px clip)、青色描边发光、"进入测试"。
- 共有状态:hover(底色一档变化,200ms,无位移)/active(降一档)/focus-visible(各方向焦点环)/disabled(--{ns}-text-3)。触控目标 ≥ 44px。

**HeroPanel 开场块**(BLK-1)
- 遵守 gpt-tasteskill 2 行铁律:标题容器用超宽容器(`max-inline-size: min(68ch, 92vw)` 级别),中文主标题 ≤ 2 行,clamp 控字号;禁徽章、禁 hero 下放数据、禁药丸标签墙。A:左对齐巨型两行 + 右侧任务队列预告;B:书名页式居中题录 + 副题;C:开机式居中 + 靶道窗渐显(静态呈现完成态)。

**Annotation 批注/图注**(B 专属原语,C 的 HUD 标签可借用)
- 结构:编号上标 + 发丝引线 + Mono 图注:"① 过冲:准星越过靶心后回拉。你的均值 +4.2%。"
- 状态:静态;focus 可达(作为链接样式锚点)。引线在 375px 收为行内括号注,不产生横向溢出。

---

## 6. Motion & Interaction

**概念是静态页面:产品行为(测试、记录、图表生成)明确 out-of-scope。** 以下仅约束原语级状态反馈,供 showcase 与产品屏使用。

| 类型 | 时长 | 缓动 | 用途 |
|---|---|---|---|
| micro | 120ms | ease-out | 按钮/行 hover、状态签切换 |
| standard | 240ms | ease-in-out | 面板折叠、tab 队列切换 |
| emphasis | 480ms | cubic-bezier(.16,1,.3,1) | 仅 C 开机渐显 / A Scorebug 入场 |

- 只动 `transform` 与 `opacity`;禁动画布局属性(高度/边距/位置用重排,不用补间)。
- 每个可交互元素必须有 hover + active + focus-visible 三态;不可交互元素**禁止**加动效(无信息量的装饰微动画按缺陷处理)。
- `prefers-reduced-motion: reduce` 时:全部动效降为 0ms 直接换态,辉光/暗角等静态氛围保留。
- A、B 默认近乎零动效(Nike/纸的平直);C 允许每屏一次签名渐显。滚动揭示类效果在概念中**禁用**(概念页短,无滚动叙事需求,避免 gpt 模板惯性)。

---

## 7. Depth & Surface

每方向**只选一种深度策略并守住**:

- **A · Broadcast Grid = 线条与色阶(mixed 的克制版)**:面板间用 1px `--bc-rule` 与 2–3% 表面台阶分层;唯一允许的阴影是 Scorebug 向上投出的 `0 -8px 24px rgba(0,0,0,.45)`(让它"浮"在网格上,是全页最高层)。无 hover 抬升。
- **B · Calibration Archive = 纯平纸**:零阴影、零圆角泛滥(标本卡 2px)。深度只来自:纸的三档色阶、发丝线、**引线(0.75px 细线连接图与注)**、以及页边注栏的物理留白。hover 不加投影,只换纸亮档底色。
- **C · Range OS = 受光舱体(tonal + rim light)**:仪表控件用 1px 顶部青色 rim 亮边 + 1px 底部 `rgba(0,0,0,.6)` 内嵌暗线制造加工金属感;靶道窗用内阴影(`inset 0 0 120px rgba(0,0,0,.8)`)+ 径向底光;全页暗角 vignette 一层。禁 blur 玻璃、禁外发光泛滥——光是从靶道里透出来的,不是糊出来的。

---

## 8. Accessibility Constraints & Accepted Debt

### 约束

- **目标 WCAG 2.2 AA**:正文对比 ≥ 4.5:1,大字/图形 ≥ 3:1(§2 表内已按此选值并标注关键比值);focus-visible 环 2px、对比 ≥ 3:1、`outline-offset: 2px`;全部交互元素键盘可达,Tab 顺序与视觉阅读序一致;触控目标 ≥ 44×44px。
- **色彩独立状态(强制)**:五类状态一律"颜色 + 形状符号 + 文字"三通道冗余(▲ ● ◆ ✕ 为 CSS/字符形状,非 emoji);任何仅靠琥珀/青/红区分的地方视为缺陷。
- **认知负荷(对应 P3 疲劳人格,COGA 取向)**:裁决永远是一句话 + 一个数字,不需要读表格才能拿到结论;每屏一个主行动;错误信息给下一步("采样不足 · 再测一轮即可"),不出现指责式措辞;三方向即使皮肤不同,BLK-5 裁决的位置与措辞结构保持一致(位置可预期 = 疲劳时可依赖)。
- **动效**:`prefers-reduced-motion` 全量降级(§6);辉光、暗角为静态,不受影响但不得闪烁(无 >3Hz 的明暗变化,闪避光敏风险)。
- **中文排印**:所有 §3 回退栈含 Noto Sans SC / Noto Serif SC;若字体 CDN 不可达,页面必须以系统 CJK 回退仍可完整阅读,不得出现豆腐块依赖(即:不依赖 Web Font 才能传达信息)。
- **内容压力测试**:每个原语须扛住空态、超长中文标签(如"极限甩距反弹复合校验")、无空格长串;375px 收敛单列、无主内容横向滚动。
- **验证门禁**:任何方向的产品屏在完成前须过 Primitive Showcase Gate(§5.1)与 `/visual-qa`(375/768/1280 + 全部原语状态 + 双 oracle);本 DESIGN.md 只是契约,不是完成证据。

### 范围声明(Out of Scope,主动划定)

- 三方向均为**纯视觉静态原型**:无测试逻辑、无输入交互、无数据持久化、不改 `app.js`/`engine.js`/`scene3d.js`;`package.json` 不动,不引入任何依赖或框架。
- 不触碰 `index.html`、`styles.css`、`ui-previews/`(用户已否决)。
- 概念文件只进 `concepts/{broadcast-grid,calibration-archive,range-os}/`。

### Accepted Debt

| 项 | 位置 | 接受理由 | 责任人 / 出口 |
|---|---|---|---|
| Google Fonts 外链在离线/内网环境不可达 | 三方向 `<link>` 引入 | 用户以回退栈验收过可读性;零构建项目无常驻字体托管 | 方向被选中并入产品时,字体本地化进 `vendor/` |
| 拉丁展示字(Saira Cond/Chakra Petch 等)无 CJK 字形,中文标题实际由 Noto 字重承担 | §3 全部 | 免费无构建约束下无理想中文窄体展示字;已用字号/字重/字距补偿并明记 | 选定方向后评估中文标题改用系统超粗字重或字形图化 |
| loading 态以静态"进行中"呈现,无真实异步 | §5.1 各原语 | 功能接线被用户明确排除 | 并入产品时由真实状态替换,showcase 保留状态槽 |
| Scorebug / 靶道窗等签构件在 375px 降级为吸底条/缩窗,非原生移动设计 | §4.2/§4.4 | 概念阶段移动端为次要场景(CS2 玩家用桌面) | 方向入选后做移动端专项 |

新债必须在本表落笔后才可被接受,禁止静默。

---

*契约结束。下一步(非本文件范围):按方向各建 `concepts/<dir>/showcase.html` → 视觉验收 → 产品屏 → `/visual-qa` 双 oracle。*
